; Kept intentionally small (10000-10100) for local/dev docker-compose port mapping -
; widen this (and the matching Dockerfile/docker-compose EXPOSE/ports ranges) for any
; deployment expecting more than a handful of concurrent calls.
[general]
rtpstart=10000
rtpend=10100
icesupport=yes
