; Simplest safe default: autoload everything the apt package installed rather than
; hand-picking modules (res_pjsip*, res_ari*, chan_pjsip, res_rtp_asterisk, res_http_websocket,
; etc.) one by one - trim this once you know exactly what you need for production.
[modules]
autoload=yes
